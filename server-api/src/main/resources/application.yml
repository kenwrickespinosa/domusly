spring:
  application:
    name: server-api
  datasource:
    url: jdbc:mysql://localhost:3306/domusly?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: Kenwrickespinosa_012404
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format-sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8080

application:
  security:
    jwt:
      secret-key: thisIsMysecregtfrdesww233eggtffeeddgkjjhhtdhttebd54ndhdhfhhhshs8877465sbbdd
      expiration: 3600000
      # refresh-token:
      #   expiration: 604800000 # 7 days

      # 180000    3 minutes
      # 3600000   1 hour
      # 86400000    a day